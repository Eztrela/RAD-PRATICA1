<%= form_with(model: contato) do |form| %>
  <% if contato.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(contato.errors.count, "error") %> prohibited this contato from being saved:</h2>

      <ul>
        <% contato.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :tipo, style: "display: block" %>
    <%= form.text_field :tipo %>
  </div>

  <div>
    <%= form.label :valor, style: "display: block" %>
    <%= form.text_field :valor %>
  </div>

  <div>
    <%= form.label :loja_id, style: "display: block" %>
    <%= form.text_field :loja_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
