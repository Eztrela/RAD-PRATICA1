<%= form_with(model: loja) do |form| %>
  <% if loja.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(loja.errors.count, "error") %> prohibited this loja from being saved:</h2>

      <ul>
        <% loja.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome, style: "display: block" %>
    <%= form.text_field :nome %>
  </div>
  <%= form.fields_for :gerente do |gerente_fields|=%>
    <div class="field">
      <%= gerente_fields.label "Nome do gerente" =%>
      <%= gerente_fields.text_field :nome =%>
    <div>
    <div class="field">
      <%= gerente_fields.label "Data de Nascimento do gerente" =%>
      <%= gerente_fields.date_field :nascimento =%>
    <div>
  <% end %>
  <div>
    <%= form.submit %>
  </div>
<% end %>
